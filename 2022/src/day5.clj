(ns day5
  (:require [inputUtils :as utils]
            [clojure.string :as str]))

(def initialCrates (list "[D] [M] [R] [J] [J] [P] [V] [P] [J]"))


(defn parseCrates [createsList]
  [])

(def moves (list "move 2 from 4 to 6"))

(let [[first-value second-value third-value] (first input)]
  (println first-value))


;;Day 5 sucks, skipping it